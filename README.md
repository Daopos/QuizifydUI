# run this for short access of later node js

# use this if you only have the standalone zip node js

- $env:Path = "C:\Users\Ivan Adrian Muñoz\Documents\node\;" + $env:Path
- npm.cmd create vite@latest my-app
- to use npm = Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
